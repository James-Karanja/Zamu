{
  "name": "zamu",
  "version": "0.1.0",
  "description": "Zamu — public money made visible, so bursaries are a right, not a favour.",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "seed": "node src/seed/run.ts",
    "test": "node --test \"test/**/*.test.ts\"",
    "typecheck": "tsc",
    "web": "node src/web/run.ts",
    "ussd": "node src/ussd/run.ts",
    "sms": "node src/sms/run.ts",
    "demo": "node src/demo/run.ts"
  },
  "devDependencies": {
    "@types/node": "^26.6.1",
    "typescript": "^7.0.2"
  },
  "license": "MIT"
}
